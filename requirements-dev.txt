# ---------------------------------------------------------------------------
# Full development environment: runtime deps (requirements.txt) plus the
# libraries needed for training, tuning, the FastAPI backend, testing and the
# EDA notebook.  Use this locally:   pip install -r requirements-dev.txt
# Deployment (Streamlit Cloud) uses the slim requirements.txt instead.
# ---------------------------------------------------------------------------

-r requirements.txt

# Visualisation (training-time plots)
seaborn>=0.12

# Hyperparameter tuning
optuna>=3.5

# Optional gradient-boosting libraries
# Comment-out if installation fails on your platform; the pipeline gracefully skips them.
xgboost>=1.7
lightgbm>=4.0

# FastAPI backend
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-multipart>=0.0.9

# Module Servo — LLM maintenance assistant (optional; offline fallback works without it)
anthropic>=0.40

# Module Servo — knowledge-base crawler (optional; offline markdown works without it).
# TF-IDF retrieval itself needs only scikit-learn (already in requirements.txt).
requests>=2.31
beautifulsoup4>=4.12

# Testing
pytest>=7.4

# Optional — manual Streamlit UI QA only (scripts/verify_dashboard_clicks.py).
# Left commented so it stays OUT of the Hugging Face image (which installs this
# file). To run that script locally: uncomment, then `playwright install chromium`.
# playwright>=1.40

# Jupyter (for the EDA notebook)
jupyter>=1.0
ipykernel>=6.27
