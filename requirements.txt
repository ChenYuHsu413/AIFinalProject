# ---------------------------------------------------------------------------
# Runtime dependencies — minimal set to RUN the Streamlit dashboard with a
# pre-trained model.  This is what Streamlit Community Cloud installs.
#
# For local training / tuning / notebooks / the FastAPI backend, install the
# full environment instead:   pip install -r requirements-dev.txt
# ---------------------------------------------------------------------------

# Core data / ML stack (model load + inference)
numpy>=1.24
# 3.0.4 的 py3.12 wheel 會在 pandas.date_range 段錯誤（CI exit 139）；3.0.3 與 py3.11 正常。排除壞版本，待 3.0.5+ 修復後自動採用。（2026-06-29）
pandas>=2.0,!=3.0.4
scikit-learn>=1.3
joblib>=1.3
PyYAML>=6.0

# Per-prediction explainability (SHAP TreeExplainer for the best tree model)
shap>=0.44

# Streamlit dashboard + interactive charts / components
streamlit>=1.30
plotly>=5.18
streamlit-shadcn-ui>=0.1.19
streamlit-option-menu>=0.4

# Needed transitively by the model-evaluation page:
# pandas Styler.background_gradient requires matplotlib.
matplotlib>=3.7

# Module B (IMS dynamic health / RUL):
# - pyarrow: read the committed ims_set2_features.parquet on the cloud demo
# - scipy: time-domain vibration features (kurtosis / skew)
pyarrow>=14.0
scipy>=1.11
